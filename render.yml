services:
  - type: worker
    name: telegram-trading-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python solana_forwarder.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11

